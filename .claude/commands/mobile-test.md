---
description: 모바일 뷰포트에서 게임 QA 테스트
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, Agent, WebFetch
---

## Context

- 프로젝트 파일: !`ls *.html *.js *.css 2>/dev/null`

## Your task

모바일 환경에서 게임이 정상 동작하는지 테스트합니다.

### 테스트 항목

1. **레이아웃**: 모바일 세로(375x667, 390x844) 뷰포트에서 UI가 잘리거나 넘치지 않는지
2. **터치 조작**: 버튼 터치 영역이 충분한지 (최소 44x44px)
3. **애니메이션**: 크레인 이동, 잡기, 결과 표시 애니메이션이 매끄러운지
4. **결과 화면**: 보상 결과 오버레이가 정상 표시되는지
5. **재시작**: "다시 하기" 버튼이 정상 동작하는지

### 방법
- /browse 스킬을 활용하여 모바일 뷰포트로 테스트
- 로컬 서버가 있으면 localhost, 없으면 배포된 GitHub Pages URL 사용
- 발견된 문제는 심각도 순으로 정리

### 결과 보고
- 통과/실패 항목 체크리스트
- 실패 항목은 스크린샷 + 수정 방안 제안
